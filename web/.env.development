# Dev only. Turnstile always passes with the Cloudflare official test keys.
# The e2e harness skips the widget entirely (fake token); the test deployment
# runs the always-pass test secret, which accepts any token.
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA
