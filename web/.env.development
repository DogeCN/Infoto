# Dev only. Turnstile always passes with the Cloudflare official test keys.
# The e2e harness skips the widget entirely (fake token); the dev deployment
# accepts it via the "Turnstile secret 未配置则放行" allow-branch.
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA
