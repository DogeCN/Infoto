name = "infoto"
main = "src/worker.js"
compatibility_date = "2025-08-01"

# 静态资源（前端 index.html）
[assets]
directory = "./public"
binding = "ASSETS"

# 照片元数据 KV（首次部署时 CI 会自动创建并回填 id）
[[kv_namespaces]]
binding = "PHOTOS"
id = "REPLACE_WITH_KV_ID"
